pub(crate) mod extractor; // Actix FromRequest implementation
                          // pub(crate) mod middleware;
                          // mod password;
                          // mod queries;
                          // mod service;
                          // mod sessions;
                          // mod types; // Actix request middleware

// pub use service::AuthService;
// pub use types::{Credentials, SessionToken};
