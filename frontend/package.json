{
  "name": "ubedtech-frontend",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "build": "node build.mjs",
    "pretest": "cd ../backend && cargo run --quiet -- render-pages ../frontend/test/pages",
    "test": "node test/a11y.mjs && node test/calendar.mjs"
  },
  "dependencies": {
    "@alpinejs/csp": "3.15.12"
  },
  "devDependencies": {
    "axe-core": "4.12.1",
    "esbuild": "0.28.1",
    "jsdom": "29.1.1"
  }
}
